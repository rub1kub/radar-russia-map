# --- Секреты. Единственное, что не публикуется по существу. ---------------
.env
ingest/.env
*.session
*.session-journal

# --- Окружения и сборки ----------------------------------------------------
node_modules/
dist/
ingest/.venv/
__pycache__/
*.tsbuildinfo
.DS_Store

# --- Рабочие данные ingest -------------------------------------------------
# Содержит файл сессии Telegram (полный доступ к аккаунту), базу radar.db и
# сырой корпус чужих сообщений. Всё восстанавливается:
#   ingest/dump_history.py -> pipeline/rebuild.py
ingest/data/

# 142 МБ — сверх жёсткого лимита GitHub в 100 МБ, а LFS-бюджет аккаунта исчерпан.
# Скачивается заново: https://storage.googleapis.com/teow-data/Ecoregions2017.zip
# (RESOLVE Ecoregions 2017, CC BY 4.0). Нужен только для land-cover.json.
research/data_sources/Ecoregions2017.zip

# Штамп пропуска prepare:data: локальное состояние, не данные
public/data/.prepare-stamp.json

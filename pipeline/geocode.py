"""Разрешение топонимов сообщения в зоны справочника.

Главная сложность — омонимы: 22 177 названий НП повторяются. Разрешаются по
контексту сообщения: если в тексте назван регион или район, кандидат из этой
ветки иерархии выигрывает. Контекст матч собирает из ОСТАЛЬНЫХ слов
сообщения, а не из себя самого, — иначе тёзка подтверждает сам себя.

Отдельный класс омонимов — украинские города: справочника с той стороны
границы нет, зато 667 российских зон носят производные от них имена. Их
разводит стеммер, а сам город без названного российского региона на карту
не попадает вовсе.
"""

from __future__ import annotations

import sqlite3
from dataclasses import dataclass
from functools import lru_cache

from .textnorm import (expand_units, form_gender, name_gender, norm_key,
                       stem_key, stem_word)

# Названия НП, совпадающие с обиходными словами. Принимаются только при
# явном контексте региона или района, иначе дают массовые ложные срабатывания.
AMBIGUOUS = {
    "мир", "победа", "восход", "заря", "новый", "новая", "central", "рассвет",
    "дружба", "родина", "звезда", "маяк", "октябрьский", "первомайский",
    "старый", "старая", "старое", "большой", "большая", "малый", "малая",
    "северный", "южный", "западный", "восточный", "центральный", "лесной",
    "луговое", "озерное", "речное", "садовое", "степное", "красное", "белое",
    "россия", "украина", "тыл", "море", "берег", "город", "район", "область",
}

LEVEL_RANK = {"region": 0, "district": 1, "place": 2}

# Лексика обстановки. Никогда не является топонимом, даже если в справочнике
# есть одноименная деревня.
STOPWORDS = {
    "опасность", "опасности", "тревога", "тревоги", "бпла", "фиксация", "фиксации",
    "фиксациям", "внимание", "меры", "мера", "безопасности", "предосторожности",
    "отбой", "работа", "пво", "сбитие", "сбит", "взрыв", "гром", "сирена",
    "направлении", "направление", "сторону", "далее", "близлежащие", "ближайшие",
    "пригород", "глубь", "угроза", "атака", "атаки", "массовая", "максимальная",
    "ракета", "ракетная", "авиация", "дрон", "дроны", "цель", "цели", "борт",
    "район", "районы", "область", "области", "край", "республика", "округ",
    "город", "поселение", "территории", "муниципальных", "образований",
    "информация", "экстренная", "беспилотная", "движение", "аэропорт",
    "соблюдать", "продолжаем", "паники", "погодные", "условия", "еще", "ещё",
    "суда", "судно", "судов", "гражданские", "побережье", "полуостров",
    "тыл", "приграничье", "акватория", "море", "залив", "мост", "аэропорту",
    # Стороны света в сводках всегда описывают направление подлёта («на восток»,
    # «с востока»), но в справочнике есть посёлок Восток — и он собирал их все.
    "восток", "запад", "север", "юг",
    # Типовые слова НП сами по себе не топоним. Двусловные имена вроде
    # «Станица Луганская» это не задевает: проверка работает только на
    # одиночном ключе, а длинная n-грамма пробуется раньше.
    "станица", "село", "хутор", "деревня", "поселок", "посёлок", "пгт",
    "микрорайон", "мкр", "аул",
    # Местоимения и вежливость из подписей каналов: в справочнике есть
    # деревни Ваша, Наша, Моя, и «ваша поддержка» уходила в них.
    "ваша", "ваши", "наша", "наши", "своя", "свои", "всем", "всех",
}

# Стеммированные варианты защитных списков. Без них «по краю», «в районе»,
# «моря», «мерами» проскочили бы мимо STOPWORDS в стеммированном проходе.
STOPWORD_STEMS = {stem_word(word) for word in STOPWORDS}
AMBIGUOUS_STEMS = {stem_word(word) for word in AMBIGUOUS}

# Физико-географические объекты справочником не покрыты, но их определения
# выглядят как топонимы. Прилагательное перед таким словом относится к нему,
# а не к одноименному хутору: «с Азовского моря» — не станица Азовская,
# «Таманского полуострова» — не посёлок Таманский, «в северной части» — не
# посёлок Северный.
FEATURE_HEADS = {
    "море", "залив", "полуостров", "мост", "побережье", "коса", "лиман",
    "водохранилище", "часть", "направление", "тэс", "аэс", "гэс", "нпз",
}
FEATURE_HEAD_STEMS = {stem_word(word) for word in FEATURE_HEADS}

# --- Украинские тёзки -------------------------------------------------------
#
# Справочник покрывает только российские зоны, но 667 из них носят имена,
# производные от украинских городов: 309 Николаевок, 39 Николаевских, 22
# Черниговки, 15 Львовых, Харьковские, Одесское, хутор Кривой Рог. Каналы
# пересказывают удары по Украине теми же словами, что и обстановку у себя,
# и разбор садился на случайного тёзку: «Пуски БПЛА от Николаевской области»
# уходило в Николаевский район Хабаровского края (8 000 км), «в Кривом Роге» —
# в хутор Орловской области, «взрывы во Львове» — в село Львове.
#
# Отбрасывать такие сообщения целиком нельзя: Николаевка в Крыму, Черниговка
# в Приморье и станица Львовская на Кубани — настоящие места настоящих
# оповещений. Значит, различать надо здесь, по контексту сообщения.

# Имена самих украинских городов. Российские тёзки у них есть, но все они —
# безвестные хутора, которых в сводке никто не назовёт без региона. Поэтому
# такой ключ принимается только при явно названном российском регионе или
# районе, внутри которого тёзка и лежит.
#
# Херсон, Запорожье и Черноморское сюда намеренно НЕ входят: Херсонская и
# Запорожская области есть в справочнике регионами, а пгт Черноморское в
# Крыму — адресат 13 живых оповещений. Требовать для них контекст значило бы
# гасить настоящие события ради чужих.
FOREIGN_CITIES = {"кривой рог", "крив рог"}
FOREIGN_CITY_STEMS = {
    "киев", "одесс", "харьков", "николаев", "чернигов", "львов",
    "днепр", "днепропетровск", "кременчуг", "полтав", "черкасс",
}

# Производные от них российские имена (Николаевка, Черниговка, Львовское,
# Харьковский, Одесское) дают ДРУГОЙ стем — «николаевк», «черниговк»,
# «львовск», «харьковск», «одесск» — и под это правило не попадают. Разводит
# их сам стеммер, отдельного списка исключений не нужно.

# Типовые слова региона. Прилагательное перед ними называет область, а не НП.
REGION_HEAD_STEMS = {stem_word(word) for word in ("область", "обл")}

# Слова-маркеры типа НП. После expand_units сокращения раскрыты в полные
# слова, поэтому маркер здесь всегда однозначен. За маркером стоит название
# населённого пункта — этого хватает, чтобы снять пороги безвестности и
# отбросить одноимённую область: «Ст. Воронежская» под Усть-Лабинском
# разрешалась в Воронежскую область за 700 км.
# Хранятся стеммами: маркер сам стоит в косвенном падеже не реже названия —
# «в станице Раевской», «в посёлке Афипском».
PLACE_MARKER_STEMS = {stem_word(word) for word in (
    "город", "поселок", "село", "станица", "хутор", "деревня", "аул",
    "слобода", "пгт", "рп", "микрорайон", "мкр",
)}

# Названия регионов, которых нет в справочнике: он хранит короткую форму
# («Чувашия», «Удмуртия»), а каналы пишут официальную или аббревиатуру.
# ДНР и ЛНР в сводках почти всегда только аббревиатурой — 303 сообщения.
REGION_ALIASES = {
    "днр": "донецкая народная республика",
    "лнр": "луганская народная республика",
    "хмао": "ханты-мансийский автономный округ — югра",
    "югра": "ханты-мансийский автономный округ — югра",
    "янао": "ямало-ненецкий автономный округ",
    "нао": "ненецкий автономный округ",
    "еао": "еврейская автономная область",
    "кбр": "кабардино-балкария",
    "кабардино-балкарская республика": "кабардино-балкария",
    "кчр": "карачаево-черкесия",
    "карачаево-черкесская республика": "карачаево-черкесия",
    "чувашская республика": "чувашия",
    "удмуртская республика": "удмуртия",
    "чеченская республика": "чечня",
    "северная осетия": "республика северная осетия-алания",
    "рсо": "республика северная осетия-алания",
    "подмосковье": "московская область",
    "спб": "санкт-петербург",
    "питер": "санкт-петербург",
}

# Физико-географические ориентиры, которых в справочнике нет вовсе, но
# каждый целиком лежит в одной админ-единице. Без них 62 сообщения про
# мост и Тамань не геокодировались ничем.
FEATURE_ALIASES = {
    "крымский мост": "керчь",
    "керченский мост": "керчь",
    "таманский полуостров": "темрюкский район",
    "керченский полуостров": "керчь",
}

# Ключи короче четырёх букв обычно мусор, но аббревиатуры регионов надо
# пропускать: «ДНР», «ЛНР», «КБР», «РСО».
#
# Порог именно четыре, а не три: у трёхбуквенных ключей на корпусе 249
# срабатываний из 425 дал предлог «над» — стеммер режет до него Надым. Цена
# порога — «в Ялте» и «Саки» в косвенном падеже, это признанный недобор.
SHORT_KEYS = {key for key in REGION_ALIASES if len(key) < 4}

# Ниже этого порога населенный пункт слишком безвестен, чтобы его называли
# одним словом в сводке. Отсекает «Примерный», «Крайний», «Ударное».
MIN_PLACE_POPULATION = 1_000

# Стеммированное одиночное слово без контекста принимается, только если за
# ключом стоит что-то заметное: регион или крупный НП. Иначе среди 2 327
# районов и 209 789 НП найдётся тёзка любому обиходному прилагательному —
# «в свободном доступе» разрешалось в городской округ Свободный.
PROMINENT_POPULATION = 20_000

# Крупный город называют вместо района, и одноимённая админ-единица в чужом
# регионе не должна его перебивать: «Донецк» в сводках — миллионник в ДНР,
# а не городской округ Донецк Ростовской области.
MAJOR_CITY_POPULATION = 100_000

# Типовые слова района: после них «Анапский» — это админ-единица, а не хутор.
# Хранятся стеммами, потому что в сводках стоят в косвенном падеже:
# «Анапского района», «по Темрюкскому округу».
DISTRICT_HEAD_STEMS = {stem_word(word) for word in ("район", "округ", "муниципалитет")}

# Окончания, которые у существительного не встречаются: «Раевскую»,
# «Анапского», «Воронежскими» — только прилагательное.
ADJECTIVE_ONLY_ENDINGS = (
    "ого", "его", "ому", "ему", "ыми", "ими",
    "ая", "яя", "ое", "ее", "ую", "юю", "ые", "ие", "ых", "их",
    "ым", "им", "ий", "ый",
)
# «-ой/-ей/-ом/-ем» несут двойную нагрузку: «Воронежской» — прилагательное,
# «Анапой», «Ростовом» — существительное. Для выбора админ-единицы этого
# хватает (в _candidates есть безопасный откат), для отсечения чужих тёзок
# нет — там нужен только ADJECTIVE_ONLY_ENDINGS.
#
# Одиночное прилагательное в косвенном падеже почти всегда называет
# административную единицу с опущенным типовым словом: «в Воронежской» —
# область, а не село Воронежское; «Краснодарского и Ставропольского» — края.
ADJECTIVE_ENDINGS = ADJECTIVE_ONLY_ENDINGS + ("ом", "ем", "ой", "ей")

MAX_NGRAM = 4


@dataclass
class Resolved:
    zone_id: str
    level: str
    name: str
    lat: float | None
    lon: float | None
    phrase: str


@dataclass
class Match:
    """Совпадение n-граммы со справочником."""

    key: str
    zone_ids: list[str]
    exact: bool
    # Исходная словоформа одиночного стеммированного матча: по её окончанию
    # видно, прилагательное это или существительное. У остальных матчей пусто.
    word: str = ""
    # Перед n-граммой стояло типовое слово НП («ст. Динская»). Явный маркер
    # снимает пороги безвестности: автор сам сказал, что это населённый пункт.
    marked: bool = False
    # Запасной стеммированный матч на той же позиции. Пробуется, только если
    # точный не дал ни одной зоны.
    alternative: "Match | None" = None

    @property
    def adjectival(self) -> bool:
        return self.word.endswith(ADJECTIVE_ENDINGS)

    @property
    def adjective_only(self) -> bool:
        """Окончание, невозможное у существительного."""
        return self.word.endswith(ADJECTIVE_ONLY_ENDINGS)


class Geocoder:
    def __init__(self, connection: sqlite3.Connection):
        self.connection = connection
        self.by_name: dict[str, list[str]] = {}
        self.by_stem: dict[str, list[str]] = {}
        self.zones: dict[str, dict] = {}
        self._load()

    def _load(self) -> None:
        # Стеммированный индекс строится здесь, а не в справочнике: zone_names
        # заполняет gazetteer, и держать там производный ключ значило бы
        # пересобирать весь справочник ради правки стеммера.
        #
        # Хранимый norm прогоняется через norm_key повторно: он идемпотентен,
        # но правила нормализации могли поменяться после сборки справочника
        # (так «туапсэ» приводится к «туапсе»), а пересобирать 222 тысячи имён
        # ради этого не нужно.
        name_sets: dict[str, set[str]] = {}
        stem_sets: dict[str, set[str]] = {}
        for row in self.connection.execute("SELECT norm, zone_id FROM zone_names"):
            norm, zone_id = norm_key(row["norm"]), row["zone_id"]
            name_sets.setdefault(norm, set()).add(zone_id)
            stem_sets.setdefault(stem_key(norm), set()).add(zone_id)

        for row in self.connection.execute(
            "SELECT id, parent_id, level, name_ru, lat, lon, population FROM zones"
        ):
            self.zones[row["id"]] = dict(row)

        self._add_aliases(name_sets, stem_sets)
        # Порядок фиксируем, чтобы разбор был воспроизводимым от запуска к запуску.
        self.by_name = {name: sorted(ids) for name, ids in name_sets.items()}
        self.by_stem = {stem: sorted(ids) for stem, ids in stem_sets.items()}

    def _add_aliases(self, name_sets: dict[str, set[str]],
                     stem_sets: dict[str, set[str]]) -> None:
        """Синонимы, которых в справочнике нет: аббревиатуры и ориентиры.

        Псевдоним ведёт на уже существующую зону, поэтому новых зон не
        появляется и цепочки родителей остаются прежними. Регион берётся
        только регионом, ориентир — только той единицей, внутри которой он
        целиком лежит, иначе псевдоним стал бы источником шума.
        """
        def region(zone: dict) -> bool:
            return zone["level"] == "region"

        def landmark(zone: dict) -> bool:
            # У ориентира цель одна и заметная: район или крупный город.
            # Иначе «Керчь» затянула бы одноимённые деревни.
            return zone["level"] != "place" or (
                zone["population"] or 0) >= PROMINENT_POPULATION

        for aliases, accept in ((REGION_ALIASES, region), (FEATURE_ALIASES, landmark)):
            for alias, target in aliases.items():
                key = norm_key(alias)
                hits = [zone_id for zone_id in name_sets.get(norm_key(target), ())
                        if accept(self.zones[zone_id])]
                if not hits:
                    continue
                name_sets.setdefault(key, set()).update(hits)
                stem_sets.setdefault(stem_key(key), set()).update(hits)

    @lru_cache(maxsize=100_000)
    def chain(self, zone_id: str) -> tuple[str, ...]:
        """Цепочка от зоны к корню: [зона, район, регион]."""
        path: list[str] = []
        current: str | None = zone_id
        seen: set[str] = set()
        while current and current not in seen:
            seen.add(current)
            path.append(current)
            zone = self.zones.get(current)
            current = zone["parent_id"] if zone else None
        return tuple(path)

    @staticmethod
    def _is_event_word(key: str, size: int, stemmed: bool) -> bool:
        """Одиночное слово из лексики обстановки — не топоним ни в каком падеже.

        Ключ стеммированного прохода уже обрезан, и стеммировать его повторно
        нельзя: stem_word не идемпотентна («мория» -> «мори» -> «мор»), от
        второго прохода под запрет попадали посторонние имена.
        """
        if size != 1:
            return False
        stem = key if stemmed else stem_word(key)
        return key in STOPWORDS or stem in STOPWORD_STEMS

    @staticmethod
    def _heads_feature(words: list[str], index: int, size: int) -> bool:
        """N-грамма — определение к стоящему следом физико-географическому объекту?

        Проверяется в обоих проходах: «Таманский полуостров» и «Крымский мост»
        стоят в сводках в именительном падеже и попадали в точный индекс мимо
        этого правила, разрешаясь в посёлок Таманский и Крымский район.

        Существительное перед объектом, наоборот, называет самостоятельное
        место: «Новороссийск НПЗ», «Славянск-на-Кубани НПЗ» — это города.
        """
        after = index + size
        if after >= len(words) or stem_word(words[after]) not in FEATURE_HEAD_STEMS:
            return False
        return words[after - 1].endswith(ADJECTIVE_ENDINGS)

    def _names_region(self, phrase: str) -> bool:
        """Стоит ли за фразой зона уровня региона — в любом из двух индексов."""
        zone_ids = list(self.by_name.get(phrase, ()))
        zone_ids += self.by_stem.get(stem_key(phrase), ())
        return any(self.zones[zone_id]["level"] == "region" for zone_id in zone_ids)

    def _heads_foreign_region(self, words: list[str], index: int, size: int) -> bool:
        """N-грамма — прилагательное чужой области перед словом «область»?

        Все российские регионы в справочнике есть, поэтому «X область», за
        которой региона не стоит, называет единицу с той стороны границы:
        Николаевскую, Харьковскую, Одесскую, Киевскую, Черниговскую. Их
        российские тёзки — Николаевский район Хабаровского края, станица
        Николаевская под Константиновском — к ней отношения не имеют, а
        разбор садился именно на них: 5 сообщений корпуса.

        Проверяется и голое прилагательное: справочник хранит ДНР и ЛНР
        республиками, а каналы пишут «Донецкая область», «Луганская область» —
        регион за словом всё-таки стоит, и гасить его нельзя.
        """
        after = index + size
        if after >= len(words) or stem_word(words[after]) not in REGION_HEAD_STEMS:
            return False
        return not (self._names_region(" ".join(words[index:after + 1]))
                    or self._names_region(" ".join(words[index:after])))

    def _lookup(self, words: list[str], index: int,
                exact: bool) -> tuple[str, list[str], int] | None:
        """Самая длинная n-грамма с позиции index, найденная в нужном индексе."""
        table = self.by_name if exact else self.by_stem
        for size in range(min(MAX_NGRAM, len(words) - index), 0, -1):
            key = " ".join(words[index:index + size])
            if not exact:
                key = stem_key(key)
            if len(key) < 4 and key not in SHORT_KEYS:
                continue
            if self._is_event_word(key, size, stemmed=not exact):
                continue
            if self._heads_feature(words, index, size):
                continue
            if self._heads_foreign_region(words, index, size):
                continue
            hits = table.get(key)
            if hits:
                return key, hits, size
        return None

    def _lookup_district(self, words: list[str],
                         index: int) -> tuple[str, list[str], int] | None:
        """«Анапский район» -> Анапа: района в справочнике нет, а город есть.

        Прилагательное от имени города строится суффиксом «-ск-» (Анапа ->
        анапский, Тихорецк -> тихорецкий), поэтому основу восстанавливаем
        обратным ходом. Ход неоднозначный, так что срабатывает он только
        последним, только перед типовым словом района и только если за
        основой стоит заметная зона — иначе любое прилагательное находило бы
        себе безвестного тёзку.
        """
        if (index + 1 >= len(words)
                or stem_word(words[index + 1]) not in DISTRICT_HEAD_STEMS):
            return None
        stem = stem_word(words[index])
        if not stem.endswith("ск") or len(stem) < 5:
            return None
        base = stem[:-2]
        hits = [zone_id for zone_id in self.by_stem.get(base, ())
                if self._is_prominent([zone_id])]
        return (base, hits, 2) if hits else None

    def _build(self, words: list[str], index: int,
               hit: tuple[str, list[str], int], exact: bool) -> Match:
        key, zone_ids, size = hit
        word = "" if exact or size != 1 else words[index]
        marked = index > 0 and stem_word(words[index - 1]) in PLACE_MARKER_STEMS
        return Match(key, zone_ids, exact, word, marked)

    def _match(self, words: list[str], index: int) -> tuple[Match, int] | None:
        """Матч с позиции index: сначала точный индекс, затем стеммированный."""
        exact_hit = self._lookup(words, index, exact=True)
        stem_hit = self._lookup(words, index, exact=False)
        hit, exact = (exact_hit, True) if exact_hit else (stem_hit, False)
        # Найденное перед словом «район» одно лишь скопление хуторов — это не
        # район: у «Анапский район» в справочнике два одноимённых хутора и ни
        # одной админ-единицы. Тогда пробуем восстановить город по основе.
        if hit is None or not any(
                self.zones[zone_id]["level"] != "place" for zone_id in hit[1]):
            fallback = self._lookup_district(words, index)
            if fallback is not None:
                hit, exact = fallback, False
        if hit is None:
            return None
        match = self._build(words, index, hit, exact)
        # Точная форма надёжнее, но в справочнике есть имена, записанные уже в
        # косвенном падеже («Лисичанске», «Бердянске» — безымянные хутора).
        # Такой тёзка перехватывал точный проход и хоронил настоящий город,
        # поэтому стеммированный вариант остаётся запасным.
        if exact and stem_hit is not None and stem_hit[0] != hit[0]:
            match.alternative = self._build(words, index, stem_hit, False)
        return match, hit[2]

    def _scan(self, phrase: str) -> list[Match]:
        """Скользящее сопоставление n-грамм, длинные совпадения приоритетнее.

        В реальных сообщениях топоним окружен мусором: «🔴Краснодар и ближайшие»,
        «Славянский район и далее в глубь». Точное совпадение всего фрагмента
        здесь не работает.

        С каждой позиции сначала пробуется точный индекс и только потом
        стеммированный: справочник в именительном падеже надёжнее огрызка.
        """
        words = norm_key(expand_units(phrase)).split()
        found: list[Match] = []
        index = 0
        while index < len(words):
            hit = self._match(words, index)
            if hit is None:
                index += 1
                continue
            match, size = hit
            found.append(match)
            index += size
        return found

    def resolve(self, phrases: list[str]) -> list[Resolved]:
        """Разрешить фразы сообщения в зоны, снимая омонимию контекстом."""
        candidates: list[Match] = []
        for phrase in phrases:
            candidates.extend(self._scan(phrase))

        contexts = self._contexts(candidates)

        resolved: list[Resolved] = []
        used: set[str] = set()
        for match, context in zip(candidates, contexts):
            best = self._pick(match, context)
            if best is None and match.alternative is not None:
                match = match.alternative
                best = self._pick(match, context)
            if best is None or best in used:
                continue
            used.add(best)
            zone = self.zones[best]
            resolved.append(Resolved(best, zone["level"], zone["name_ru"],
                                     zone["lat"], zone["lon"], match.key))
        return resolved

    def _share(self, match: Match) -> set[str]:
        """Что этот матч сообщает о географии сообщения.

        Контекстом делятся только уверенные матчи: он единственное, чем
        снимается омонимия, и ошибка здесь размножается на всё сообщение.
        Для района порог прежний — «почти нет тёзок»: районов 2 327, и их
        имена сталкиваются с именами НП постоянно.

        Регион засчитывается и тогда, когда имя делят с ним десятки деревень:
        «Крым» — это Республика Крым, а не одна из пяти одноимённых деревень
        в Тамбовской, Ростовской, Омской, Брянской областях и Приморье.
        Только в именительном падеже: обрезок стеммера («самарск» из
        «Самарской») делит ключ со всеми Самарскими сёлами сразу, и на
        перечислении полутора десятков областей в сводке Минобороны такой
        контекст открывал дорогу любой безвестной деревне внутри них.
        """
        regions = [zone_id for zone_id in match.zone_ids
                   if self.zones[zone_id]["level"] == "region"]
        # Два региона за одним ключом — уже не «однозначно названный»:
        # «Алтай» это и Республика Алтай, и Алтайский край.
        if match.exact and len(regions) == 1:
            return set(self.chain(regions[0]))
        if len(match.zone_ids) > 2:
            return set()
        return {ancestor for zone_id in match.zone_ids
                if self.zones[zone_id]["level"] in ("region", "district")
                for ancestor in self.chain(zone_id)}

    def _contexts(self, candidates: list[Match]) -> list[set[str]]:
        """Для каждого матча — контекст, собранный ОСТАЛЬНЫМИ словами.

        Свой собственный вклад матч не получает. Иначе одноимённый регион сам
        себя подтверждает и забирает бонус контекста у населённого пункта,
        который на самом деле назвали: «Ленинградская, Каневская, Тихорецк,
        Тимашевск» — это станицы Кубани, а разбор уезжал в Ленинградскую
        область за 1 500 км. Теперь Тихорецкий район ставит контекстом
        Краснодарский край, и станица Ленинградская выигрывает у области.

        Так же снимается и вторая ошибка: «Петропавловский район. Воронежская
        область» разрешалось в Петропавловский район Алтайского края — район
        подтверждал сам себя, и названная область его уже не перевешивала.

        Отказ от своего вклада только разводит тёзок и никогда не отбирает у
        матча единственную опору: если внешнего свидетельства о нём нет вовсе
        («Тихорецк» одним словом), матч остаётся сам себе свидетелем — иначе
        одиночное оповещение районной ленты перестаёт геокодироваться.

        «Остальные» считаются по ключу, а не по позиции: повторённое в
        сообщении слово — тот же свидетель, а не второй. По позиции два
        одинаковых матча подтверждали друг друга и возвращали себе бонус,
        который правило и снимает: «Ленинградская, Ленинградская, Каневская,
        Тихорецк» снова уезжало в область за 1 500 км, а «Петропавловский
        район» дважды подряд — обратно в Алтайский край.
        """
        shares = [self._share(match) for match in candidates]
        total: set[str] = set().union(*shares) if shares else set()
        by_key: dict[str, set[str]] = {}
        for match, share in zip(candidates, shares):
            by_key.setdefault(match.key, set()).update(share)
        # Ключей в сообщении единицы, поэтому объединение считается по ним, а
        # не по каждому матчу заново.
        outside = {key: set().union(*[share for name, share in by_key.items()
                                      if name != key])
                   for key in by_key}

        contexts: list[set[str]] = []
        for match in candidates:
            external = outside[match.key]
            supported = any(set(self.chain(zone_id)) & external
                            for zone_id in match.zone_ids)
            contexts.append(external if supported else total)
        return contexts

    def _candidates(self, match: Match) -> list[str]:
        """Зоны-кандидаты матча: для одиночного прилагательного — только админ.

        Иначе выигрывает случайный одноименный хутор, попавший в контекстный
        регион: «от Воронежской и Тамбовской областей» разрешалось в село
        Воронежское Тамбовской области вместо Воронежской области.
        """
        if not match.adjectival:
            return match.zone_ids
        admin = [zone_id for zone_id in match.zone_ids
                 if self.zones[zone_id]["level"] != "place"]
        return admin or self._agreeing(match, match.zone_ids)

    def _agreeing(self, match: Match, zone_ids: list[str]) -> list[str]:
        """Оставить НП, у которых род имени сходится с родом словоформы.

        «в станице Раевской» — это Раевская под Новороссийском, а не посёлок
        Раевский в Башкортостане, куда разбор уходил за 1 600 км: стеммер даёт
        обоим ключ «раевск». Админ-единицы не трогаем — там род определяется
        типовым словом («Воронежская область»), а не именем.

        Если род не определён или ни один кандидат не согласуется, список
        возвращается целиком: правило только разводит тёзок, но никогда не
        отбирает единственный вариант.
        """
        gender = form_gender(match.word)
        if not gender:
            return zone_ids
        agreeing = [
            zone_id for zone_id in zone_ids
            if self.zones[zone_id]["level"] != "place"
            or name_gender(self.zones[zone_id]["name_ru"]) == gender
        ]
        return agreeing or zone_ids

    def _is_prominent(self, zone_ids: list[str]) -> bool:
        """Есть ли за ключом регион или крупный НП, узнаваемый без контекста."""
        for zone_id in zone_ids:
            zone = self.zones[zone_id]
            if zone["level"] == "region":
                return True
            if (zone["population"] or 0) >= PROMINENT_POPULATION:
                return True
        return False

    def _is_noun_form(self, key: str) -> bool:
        """Обрезок совпал с полным именем справочника — значит, это оно и есть.

        «над Краснодаром» и «над Белгородом» стеммер режет до «краснодар» и
        «белгород», а это готовые имена зон: существительное в косвенном
        падеже. У прилагательного так не выходит — «в свободном» даёт
        «свободн», такого имени нет, и правило его не пропускает.
        """
        return key in self.by_name

    def _pick(self, match: Match, context: set[str]) -> str | None:
        key = match.key
        single = " " not in key
        # Стеммированное одиночное слово — самый рискованный класс: «Мирное»,
        # «Мирный» и «Мира» сходятся в один ключ.
        weak = single and not match.exact

        # Ключ стеммированного прохода уже обрезан, второй проход запрещён:
        # см. оговорку про идемпотентность в _is_event_word.
        stem = key if not match.exact else stem_word(key)
        ambiguous = key in AMBIGUOUS or (single and stem in AMBIGUOUS_STEMS)
        # Имя украинского города. Стеммер сводит к нему только сам город и его
        # падежи — «Николаеву», «во Львове», «по Харькову»; производные
        # российские имена (Николаевка, Львовское, Харьковский) дают другой
        # стем и сюда не попадают. Требуем регион или район в сообщении, как и
        # для обиходных слов: без него это почти всегда чужая сторона.
        foreign = key in FOREIGN_CITIES or (single and stem in FOREIGN_CITY_STEMS)

        candidates = self._candidates(match)
        # Явный маркер типа НП («х. Недвиговка») отменяет обе догадки: автор
        # прямо сказал, что дальше идёт населённый пункт. Одноимённые области
        # и районы при этом отбрасываются — «Ст. Воронежская» под
        # Усть-Лабинском разрешалась в Воронежскую область за 700 км.
        if match.marked:
            places = [zone_id for zone_id in candidates
                      if self.zones[zone_id]["level"] == "place"]
            covering = {ancestor for zone_id in places
                        for ancestor in self.chain(zone_id)}
            # Одноимённую единицу, внутри которой этот НП и лежит, оставляем:
            # «г. Краснодар» — это городской округ Краснодар, и дробить одно
            # место на две зоны нельзя, иначе события перестанут сливаться.
            candidates = [zone_id for zone_id in candidates
                          if zone_id in covering] or candidates

        # Прилагательное в косвенном падеже, за которым в справочнике стоят
        # только НП, без контекста почти всегда чужой тёзка: «через Раевскую»
        # под Новороссийском разрешалось в Раевский в Башкортостане (1 600 км).
        stray_adjective = match.adjective_only and all(
            self.zones[zone_id]["level"] == "place" for zone_id in candidates)
        prominent = weak and not stray_adjective and (
            self._is_prominent(candidates) or self._is_noun_form(key))
        admin_ids = {zone_id for zone_id in candidates
                     if self.zones[zone_id]["level"] != "place"}

        scored: list[tuple[float, str]] = []
        for zone_id in candidates:
            zone = self.zones[zone_id]
            in_context = bool(set(self.chain(zone_id)) & context)

            # Маркер здесь ничего не меняет. Он говорит «дальше населённый
            # пункт», но не говорит КАКОЙ, а у обиходного слова тёзок сотни:
            # «посёлок Победа» без контекста уходил в Побэду на 562 жителя в
            # ДНР, «село Родина» — в Башкортостан. Промах на полторы тысячи
            # километров хуже пустоты, поэтому такому имени по-прежнему нужен
            # регион или район в сообщении.
            # Маркер не спасает и украинский город: «г. Николаев» без региона
            # это порт на Южном Буге, а не хутор Николаев в Ростовской области.
            if (ambiguous or foreign) and not in_context:
                continue
            if weak and not in_context and not prominent and not match.marked:
                continue

            # Одиночное слово, совпавшее с безвестной деревней и не поддержанное
            # контекстом, — почти всегда шум: «Суда», «Север», «Победа».
            # Для стеммированного слова контекст от порога не спасает: «примерно»
            # и «крайне» тоже попадают в контекстный регион.
            if (single and zone["level"] == "place" and not match.marked
                    and (zone["population"] or 0) < MIN_PLACE_POPULATION
                    and (weak or not in_context)):
                continue

            score = 0.0
            if in_context:
                score += 1_000_000
            # Административные единицы надежнее одноименных деревень. Но
            # крупный город — не деревня: если одноимённая админ-единица его
            # не содержит, она из другого региона и проигрывает. Так «Донецк»
            # достаётся миллионнику в ДНР, а не городскому округу Донецк
            # Ростовской области, а «Краснодар» по-прежнему городскому округу
            # Краснодар — он город и содержит.
            rank = LEVEL_RANK[zone["level"]]
            if (rank == LEVEL_RANK["place"]
                    and (zone["population"] or 0) >= MAJOR_CITY_POPULATION
                    and not (admin_ids & set(self.chain(zone_id)))):
                rank = LEVEL_RANK["district"]
            score += (2 - rank) * 10_000
            score += min(zone["population"] or 0, 500_000) / 100
            # Штрафовать стеммированное совпадение здесь бессмысленно: скоринг
            # идёт внутри одного матча, где match.exact общий для всех зон, и
            # константа не меняет порядок. Приоритет точной формы обеспечивает
            # порядок проходов в _match.
            scored.append((score, zone_id))

        if not scored:
            return None
        scored.sort(key=lambda item: (-item[0], item[1]))
        return scored[0][1]

    def primary(self, resolved: list[Resolved]) -> Resolved | None:
        """Самая специфичная зона наблюдения."""
        if not resolved:
            return None
        return sorted(resolved, key=lambda item: (-LEVEL_RANK[item.level],))[0]

    def zone_path(self, zone_id: str) -> list[str]:
        return list(self.chain(zone_id))
